# 本地单机版五子棋开发规划

## 一、版本目标

本地单机版的目标是先做出一个可以在同一台设备上双人轮流下棋的五子棋网页。它不需要账号、服务器或联网功能，用户打开网页即可开始游戏。

这个版本将作为后续在线房间版的基础，因此棋盘状态、胜负判断和界面结构要写得清楚，方便以后迁移到 WebSocket 后端。

## 二、第一版功能范围

必须实现：

- 15×15 五子棋棋盘。
- 黑白双方轮流落子。
- 只能在空位落子。
- 自动判断横、竖、斜四个方向的五连。
- 显示当前回合。
- 显示胜负结果。
- 高亮最后一步。
- 高亮获胜五连。
- 重新开始。
- 人机对战模式。
- 人机对战中选择 AI 模型。

增强体验：

- 悔棋一步。
- 选择双人或人机模式。
- 人机模式下选择玩家执黑或执白。
- 人机模式下选择快速规则 AI 或浅层搜索 AI。
- 人机模式下可以选择本地 Python 服务提供的强力搜索 AI 或 CNN AI。
- 对局步数统计。
- 黑白比分统计。
- 和棋判断。
- 鼠标悬停预览落子。
- 适配手机和桌面屏幕。

暂不实现：

- 在线房间。
- 禁手规则。
- 账号系统。
- 棋谱保存。
- 计时器。

## 三、玩法规则

第一版采用自由规则：

- 黑棋先手。
- 白棋后手。
- 双方轮流落子。
- 任意一方先连成五颗同色棋子获胜。
- 长连也按胜利处理。
- 不设置禁手。
- 棋盘下满且无人获胜则为和棋。

## 四、页面结构

页面由三部分组成：

1. 顶部标题区：显示游戏名称和简短状态。
2. 主游戏区：左侧或上方为棋盘，右侧或下方为控制面板。
3. 信息面板：显示当前回合、步数、比分、操作按钮和规则提示。

桌面端采用棋盘加侧边面板布局；移动端改为上下排列。

## 五、视觉设计方向

整体风格应该清晰、温和、有棋类游戏的质感。

设计要点：

- 棋盘使用暖木色背景。
- 棋盘线条使用深棕色。
- 黑棋使用深色径向渐变，呈现棋子光泽。
- 白棋使用浅色径向渐变，并加边缘阴影。
- 当前玩家用颜色标识。
- 最后一步用小圆点标记。
- 获胜棋子用金色光环突出。
- 操作面板保持简洁，不做复杂装饰。

## 六、技术方案

使用纯前端静态页面，并按职责拆分文件：

- `index.html`：页面结构和资源引用。
- `styles/`：分区样式。
- `scripts/config.js`：棋盘大小、棋子常量、文案和星位配置。
- `scripts/state.js`：棋盘状态、落子历史、重开和悔棋状态处理。
- `scripts/rules.js`：合法落子和胜负判断。
- `scripts/ui.js`：棋盘渲染、面板渲染和页面元素管理。
- `scripts/main.js`：应用入口和事件绑定。

选择纯前端的原因：

- 不需要安装依赖。
- 可以直接在浏览器打开。
- 便于快速验证玩法。
- 后续可以把规则逻辑迁移到服务器。

## 七、核心数据结构

棋盘状态：

```text
board[row][col]

0 = 空位
1 = 黑棋
2 = 白棋
```

游戏状态：

```text
currentPlayer: 当前玩家
gameOver: 游戏是否结束
winner: 胜利方
moves: 落子历史
scores: 黑白比分
winningLine: 获胜五连坐标
```

## 八、胜负判断方案

每次落子后，只检查该棋子相关的四个方向：

- 横向：`[0, 1]`
- 纵向：`[1, 0]`
- 左上到右下：`[1, 1]`
- 右上到左下：`[1, -1]`

检查方式：

1. 从当前落子向一个方向连续寻找同色棋子。
2. 再向相反方向连续寻找同色棋子。
3. 两边加上当前棋子，如果总数大于或等于 5，则判定胜利。
4. 保存其中连续五颗或更多棋子的坐标，用于高亮显示。

## 九、交互流程

### 开始游戏

打开页面后自动初始化空棋盘，黑棋先手。

### 玩家落子

用户点击棋盘空位：

1. 判断游戏是否已经结束。
2. 判断该位置是否为空。
3. 放置当前玩家棋子。
4. 记录落子历史。
5. 检查是否获胜。
6. 如果获胜，显示结果并更新比分。
7. 如果未获胜但棋盘已满，显示和棋。
8. 如果游戏继续，切换玩家。

### 悔棋

点击悔棋：

1. 如果已经有落子，则撤销最后一步。
2. 清除胜负状态。
3. 回到被撤销棋子的玩家回合。
4. 重新渲染棋盘。

如果撤销的是已经获胜的最后一步，比分会同步回滚，避免同一局重复计分。

### 重新开始

点击重新开始：

1. 清空棋盘。
2. 清空落子历史。
3. 黑棋先手。
4. 保留比分。

## 十、文件交付

本地单机版将交付以下文件：

```text
local-gomoku/
  index.html
  styles/
    base.css
    layout.css
    board.css
    panel.css
    responsive.css
  scripts/
    config.js
    state.js
    rules.js
    ui.js
    main.js
```

用户可以直接打开 `index.html` 开始游戏。

## 十一、后续升级方向

本地版完成后，可以继续升级：

- 加入人机对战。
- 加入计时器。
- 加入禁手规则。
- 保存棋谱到本地。
- 回放整局棋。
- 改造成在线房间版。
- 加入 WebSocket 实时同步。

本地版的核心价值是先把棋盘、规则和交互打磨稳定，为后续联网版本打基础。
