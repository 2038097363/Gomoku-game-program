# 五子棋网站软件规划

## 一、项目目标

本项目计划开发一个可以和朋友在线下五子棋的网站。用户打开网页后，可以创建房间、邀请朋友进入、双方轮流落子，并由系统自动判断胜负。

第一版应优先做到“能稳定和朋友下棋”，而不是一开始就追求复杂功能。核心体验是：进入网站快、创建房间简单、棋盘清晰、落子流畅、胜负判断准确。

## 二、目标用户

主要用户是想和朋友在线对弈的普通玩家，特点包括：

- 不希望安装软件，只想通过浏览器打开。
- 希望把房间链接发给朋友即可开始。
- 需要简单清楚的棋盘和操作提示。
- 对复杂竞技规则需求不高，第一版可以先使用自由规则。

后续也可以面向进阶玩家加入禁手、计时、战绩、观战、人机对战等功能。

## 三、核心玩法规则

第一版建议采用自由规则：

- 使用 15×15 棋盘。
- 黑棋先手，白棋后手。
- 双方轮流落子。
- 只能在空交叉点落子。
- 任意一方先在横、竖或斜方向形成连续五颗同色棋子，即获胜。
- 棋盘下满无人获胜，则判定为和棋。
- 第一版暂不加入禁手。

这样可以降低开发难度，也便于用户理解。

## 四、第一版核心功能

### 1. 首页

首页应非常直接，提供两个主要入口：

- 创建房间。
- 加入房间。

首页可以展示网站名称、简短说明和当前玩法，例如“15 路自由规则”。

### 2. 创建房间

用户点击“创建房间”后，系统生成一个唯一房间号或房间链接。

创建者进入房间后，等待朋友加入。房间页面应显示：

- 房间号。
- 邀请链接。
- 复制链接按钮。
- 当前等待状态。

### 3. 加入房间

朋友可以通过两种方式加入：

- 点击邀请链接直接进入。
- 在首页输入房间号进入。

如果房间不存在、已满或对局已结束，页面应给出清楚提示。

### 4. 在线对弈

对局页面是网站核心，应包含：

- 15×15 棋盘。
- 黑白双方信息。
- 当前轮到谁下。
- 落子提示。
- 胜负结果弹窗或提示区域。
- 重新开始按钮。
- 返回首页按钮。

对局过程中，只有轮到当前玩家时才允许落子。

### 5. 自动胜负判断

每次落子后，系统检查该棋子所在位置的四个方向：

- 横向。
- 纵向。
- 左上到右下斜向。
- 右上到左下斜向。

如果任一方向连续同色棋子数量达到 5，则立即判胜。

### 6. 重新开始

一局结束后，可以提供“再来一局”功能。

建议第一版采用简单机制：

- 任意一方点击重新开始后，向另一方显示确认。
- 双方确认后清空棋盘，重新开始。
- 黑白双方可以保持不变。

如果想更简单，第一版也可以先做“房主点击重新开始”，后续再加双方确认。

## 五、推荐页面结构

网站可以包含以下页面：

| 页面 | 路径示例 | 作用 |
| --- | --- | --- |
| 首页 | `/` | 创建房间、输入房间号 |
| 房间页 | `/room/:roomId` | 在线对弈、等待朋友、显示结果 |
| 404 页面 | `/404` | 房间不存在或页面不存在 |

第一版可以只做首页和房间页，错误提示直接在页面内显示。

## 六、界面设计建议

整体界面应清晰、安静、适合长时间对弈。

### 1. 棋盘设计

棋盘是最重要的区域：

- 使用浅木色或柔和背景。
- 棋盘线条要清楚。
- 棋子黑白对比明显。
- 鼠标悬停时可以显示半透明预落子提示。
- 已落棋子不能再次点击。
- 获胜后可以高亮五连棋子。

### 2. 信息区设计

棋盘旁边或上方显示对局信息：

- 当前回合。
- 我方颜色。
- 对手状态。
- 房间号。
- 复制邀请链接按钮。
- 对局结果。

在手机屏幕上，信息区应放在棋盘上方或下方，避免挤压棋盘。

### 3. 响应式适配

网站应支持电脑和手机：

- 电脑端棋盘居中显示，旁边放信息面板。
- 手机端棋盘宽度跟随屏幕，信息面板上下排列。
- 棋盘格子大小要足够点击，避免误触。

## 七、技术方案

### 1. 前端技术

推荐使用：

- React 或 Vue 构建界面。
- TypeScript 提高代码稳定性。
- Canvas 或 HTML/CSS 绘制棋盘。
- WebSocket 实现实时对战。

如果追求开发简单，可以使用 React + CSS Grid 绘制棋盘。棋盘只有 15×15，共 225 个点，性能压力很小。

### 2. 后端技术

推荐使用：

- Node.js。
- Express 或 Fastify 提供 HTTP 接口。
- Socket.IO 或原生 WebSocket 实现实时通信。

后端负责：

- 创建房间。
- 管理玩家连接。
- 同步落子。
- 判断当前回合。
- 校验落子是否合法。
- 广播棋盘状态。
- 处理断线和重连。

### 3. 数据存储

第一版可以先使用内存存储房间数据，不接数据库。

内存数据结构示例：

- 房间 ID。
- 棋盘状态。
- 黑方玩家 ID。
- 白方玩家 ID。
- 当前回合。
- 游戏状态。
- 胜利方。
- 创建时间。

如果网站上线并需要长期运行，再考虑加入 Redis 或数据库。

### 4. 部署方案

第一版可以部署到支持 Node.js 的平台，例如：

- Render。
- Railway。
- Fly.io。
- 自己的云服务器。

如果前后端分离：

- 前端可部署到 Vercel、Netlify 或静态服务器。
- 后端部署到支持 WebSocket 的 Node.js 服务。

如果想降低部署复杂度，也可以把前端和后端放在同一个 Node.js 项目中。

## 八、核心数据结构设计

### 1. 棋盘数据

可以使用二维数组表示棋盘：

```text
0 表示空位
1 表示黑棋
2 表示白棋
```

示例：

```text
board[row][col] = 0 | 1 | 2
```

其中 `row` 和 `col` 的范围都是 `0` 到 `14`。

### 2. 房间数据

房间对象可以包含：

```text
roomId: 房间号
players: 玩家列表
blackPlayerId: 黑方玩家
whitePlayerId: 白方玩家
board: 棋盘二维数组
currentTurn: 当前回合颜色
status: waiting | playing | finished
winner: black | white | draw | null
lastMove: 最后一步
createdAt: 创建时间
```

### 3. 玩家数据

玩家对象可以包含：

```text
playerId: 玩家唯一 ID
socketId: 当前连接 ID
name: 玩家昵称
color: black | white | spectator
connected: 是否在线
```

## 九、实时通信事件设计

可以使用以下 WebSocket 事件：

| 事件名 | 方向 | 作用 |
| --- | --- | --- |
| `createRoom` | 客户端到服务端 | 创建房间 |
| `roomCreated` | 服务端到客户端 | 返回房间号 |
| `joinRoom` | 客户端到服务端 | 加入房间 |
| `roomState` | 服务端到客户端 | 同步完整房间状态 |
| `makeMove` | 客户端到服务端 | 玩家请求落子 |
| `moveRejected` | 服务端到客户端 | 落子失败原因 |
| `gameOver` | 服务端到客户端 | 对局结束 |
| `restartRequest` | 客户端到服务端 | 请求重新开始 |
| `restartGame` | 服务端到客户端 | 重置棋盘 |
| `playerDisconnected` | 服务端到客户端 | 玩家断线 |
| `playerReconnected` | 服务端到客户端 | 玩家重连 |

第一版可以先实现 `createRoom`、`joinRoom`、`roomState`、`makeMove`、`gameOver`。

## 十、开发阶段规划

### 第一阶段：本地单机版

目标：先做出可以在同一台电脑上轮流下的五子棋。

任务：

- 搭建前端项目。
- 绘制 15×15 棋盘。
- 实现黑白轮流落子。
- 实现胜负判断。
- 实现重新开始。
- 优化棋盘样式和移动端布局。

完成标准：

- 两个人可以在同一个浏览器里轮流下完整一局。
- 系统能正确判断横、竖、斜四个方向的五连。

### 第二阶段：在线房间版

目标：实现朋友之间通过链接在线对战。

任务：

- 搭建 Node.js 后端。
- 引入 WebSocket 或 Socket.IO。
- 实现创建房间。
- 实现加入房间。
- 服务端维护棋盘状态。
- 服务端校验落子是否合法。
- 双方实时同步棋盘。
- 显示等待、对局中、已结束状态。

完成标准：

- 一名用户创建房间，另一名用户通过链接加入。
- 双方在不同浏览器中可以实时下棋。
- 不能越回合落子。
- 不能覆盖已有棋子。
- 胜负结果两边同步显示。

### 第三阶段：体验优化

目标：让网站更像一个完整产品。

任务：

- 加入昵称。
- 加入复制邀请链接。
- 加入观战模式。
- 加入断线重连。
- 高亮最后一步。
- 高亮获胜五连。
- 增加音效开关。
- 增加悔棋请求。
- 增加再来一局确认。

完成标准：

- 用户不需要说明也能顺利创建房间并邀请朋友。
- 对局状态清晰，异常情况有提示。

### 第四阶段：进阶玩法

目标：增加更多玩法形式。

可选功能：

- 人机对战。
- 禁手规则。
- 计时器。
- 排行榜。
- 历史战绩。
- 账号系统。
- 棋谱保存。
- 复盘功能。
- 多种棋盘主题。
- 移动端 PWA 安装。

这些功能不建议第一版全部加入，应在核心在线对战稳定后再逐步扩展。

## 十一、功能优先级

### 必须实现

- 创建房间。
- 加入房间。
- 15×15 棋盘。
- 黑白轮流落子。
- 合法性校验。
- 胜负判断。
- 实时同步。
- 对局结束提示。

### 应该实现

- 复制邀请链接。
- 重新开始。
- 玩家昵称。
- 当前回合提示。
- 最后一步标记。
- 手机适配。

### 可以后做

- 观战模式。
- 聊天。
- 悔棋。
- 计时。
- 禁手规则。
- 账号和战绩。
- 人机对战。
- 排行榜。

## 十二、主要风险与解决方案

### 1. 网络同步问题

风险：两边棋盘状态不一致。

解决方案：以后端状态为准。客户端只发送落子请求，最终棋盘由服务端广播。

### 2. 玩家重复落子

风险：玩家快速点击导致重复提交。

解决方案：服务端检查当前回合、位置是否为空、游戏是否进行中。

### 3. 房间无人清理

风险：大量过期房间占用内存。

解决方案：定期清理超过一定时间未活动的房间，例如 2 小时无操作自动删除。

### 4. 断线问题

风险：用户刷新页面或网络波动后无法继续。

解决方案：第一版可提示对手断线；后续加入重连 token，让玩家刷新后回到原座位。

### 5. 移动端误触

风险：手机棋盘较小，用户容易点错。

解决方案：棋盘自适应宽度，落点区域足够大；可以加入点击预览或撤销确认，但第一版不必复杂化。

## 十三、推荐最小可行版本

如果希望尽快做出来，最小可行版本可以只包含：

1. 首页创建房间。
2. 通过链接加入房间。
3. 两人在线下棋。
4. 服务端判断合法落子。
5. 服务端判断胜负。
6. 显示当前回合和胜负结果。
7. 一局结束后可以重新开始。

这个版本功能不多，但已经能满足“和朋友下五子棋的网站”的核心目标。

## 十四、建议项目目录结构

如果使用 React + Node.js + Socket.IO，可以采用以下结构：

```text
gomoku-web/
  client/
    src/
      components/
        Board.tsx
        Stone.tsx
        GamePanel.tsx
      pages/
        HomePage.tsx
        RoomPage.tsx
      game/
        rules.ts
        types.ts
      styles/
        main.css
  server/
    src/
      index.ts
      rooms.ts
      rules.ts
      types.ts
  package.json
  README.md
```

也可以先做更简单的单项目结构，等功能变复杂后再拆分。

## 十五、开发顺序建议

建议按以下顺序推进：

1. 先实现单机棋盘和胜负判断。
2. 再把棋盘状态移动到服务端。
3. 加入房间和实时同步。
4. 补充房间链接、复制按钮、错误提示。
5. 做移动端适配和视觉优化。
6. 再考虑观战、悔棋、聊天、禁手等扩展功能。

这样每一步都有可运行成果，不容易在一开始被复杂功能拖慢。

## 十六、总结

这个五子棋网站第一版的核心不是“大而全”，而是让两个人能方便地通过网页开始一局棋。

推荐第一版采用自由规则、15×15 棋盘、房间链接邀请、WebSocket 实时同步和服务端胜负判断。等基础对战稳定后，再逐步加入昵称、重新开始、观战、计时、禁手、人机和战绩系统。
